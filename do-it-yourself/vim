# all

See https://github.com/ycm-core/YouCompleteMe#linux-64-bit
See also `:checkhealth provider`

```sh
sudo pacman -S cmake vim python python-pip python2-pip cmake rust go nodejs yarn ruby-irb mono

# python
sudo pip3 install --upgrade pynvim
sudo pip2 install --upgrade pynvim

# nodejs
yarn global add neovim

# ruby
gem install neovim

cd ~/.config/nvim/plugged/YouCompleteMe
#./install.py --clang-completer --clangd-completer --go-completer --rust-completer --ts-completer --cs-completer
./install.py --all
```

You might need a `~/.tern-config` file for js/ts - see: https://github.com/ain/.vim/issues/46#issuecomment-381189916
